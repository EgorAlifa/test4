import { Dictionary } from "../types/global";
export interface Options {
    global?: Dictionary<string>;
    unsafe?: boolean;
}
declare const options: Required<Options>;
export default options;
