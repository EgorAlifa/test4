diff --git a/node_modules/vite-plugin-vue2/node_modules/consolidate/lib/consolidate.js b/node_modules/vite-plugin-vue2/node_modules/consolidate/lib/consolidate.js
index 5a86ae5..19c4397 100644
--- a/node_modules/vite-plugin-vue2/node_modules/consolidate/lib/consolidate.js
+++ b/node_modules/vite-plugin-vue2/node_modules/consolidate/lib/consolidate.js
@@ -1786,6 +1786,36 @@ exports.twing.render = function(str, options, cb) {
     }
   });
 };
+
+/**
+ * Posthtml string support.
+ */
+
+exports['posthtml-tag'] = fromStringRenderer('posthtml-tag');
+exports['posthtml-bem'] = fromStringRenderer('posthtml-bem');
+
+const posthtmlRender = function(source, options, cb, type = 'bem') {
+  const posthtmlOptions = require('@goodt/posthtml-config')[type];
+  const posthtml = require('posthtml')(posthtmlOptions.plugins);
+  const processPosthtml = (content) => posthtml.process(content, { sync: true });
+  const engine = requires.posthtml || (requires.posthtml = processPosthtml);
+
+  try {
+    const { html } = cache(options) ?? cache(options, engine(source));
+    cb(null, html);
+  } catch (err) {
+    cb(err);
+  }
+};
+
+exports['posthtml-bem'].render = function(source, options, cb) {
+  return posthtmlRender(source, options, cb, 'bem');
+}
+
+exports['posthtml-tag'].render = function(source, options, cb) {
+  return posthtmlRender(source, options, cb, 'tag');
+}
+
 /**
  * expose the instance of the engine
  */
