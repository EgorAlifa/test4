const { isProd } = require('@goodt-webpack/common/utils');

module.exports.build = (config) => {
    config.isProd ??= isProd();
    const resolvedConfig = require('./config/webpack.prod')(config);
    return (/* webpackConfig */) => resolvedConfig;
};
