const { merge } = require('webpack-merge');

const commonConfig = require('./webpack.common.js');

const developmentConfig = ({ baseDir }) => ({
});

module.exports = (config) => merge(commonConfig(config), developmentConfig(config));
