const Aliases = require('./aliases');

module.exports.CORE = Object.fromEntries(
  ['goodt-wcore', ...Object.keys(Aliases)].map((alias) => [alias, alias])
);

module.exports.VENDORS = {
  'vue': ['vue', 'default']
};