const path = require('path');
const { argv } = require('./utils');

const ARGV_OUTPUT_DIR = 'output-dir';

module.exports = {
  WCORE: 'goodt-wcore',
  // npm run build options

  // @todo delete in future for compatibility patches in releases
  RESOLVER_MODULE_NAME: 'resolver',

  ARGV_DEV_TOOLS: 'devtools',
  ARGV_SOURCE_MAP: 'source-map',
  ARGV_OUTPUT_DIR,

  OUTPUT_DIR: path.join(process.cwd(), argv(ARGV_OUTPUT_DIR) ?? 'dist'),
}