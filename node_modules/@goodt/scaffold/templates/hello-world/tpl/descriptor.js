/* eslint-disable no-restricted-syntax */
import panels from '[[{panelPath}]]';

/**
 * @return {ElemDescriptor}
 */
export const descriptor = () => ({
    props: {
        message: {
            type: String,
            default: 'Hello World!',
            label: 'Message',
            hint: 'I\'am a useful hint'
        }
    }
});

export const meta = {
    descriptor,
    panels
}

export default descriptor;
