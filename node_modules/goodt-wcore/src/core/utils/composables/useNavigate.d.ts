import { NavigateOptions } from '@goodt-wcore/managers/RouteManager';

export function useNavigate(): {
    navigate: ({ url, route }: { url?: string, route?: NavigateOptions }, { isNewWindow }?: { isNewWindow?: boolean }) => void;
};
