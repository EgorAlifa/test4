import { IElemInstance } from '@goodt-wcore/elem';

export interface ISubscribeEventBusElemPlugin {
    install(vm: IElemInstance);
}

export const useSubscribeEventBus: (vm: IElemInstance) => ISubscribeEventBusElemPlugin;

// prettier-ignore
export function registerEventBusEventTypeResolvers(
    vm: IElemInstance
): void;

export class FakeEventBusWrapper {
    listen();
    trigger();
}

