import { StoreLocalState } from '@goodt-wcore/managers';
import { ElemStoreMetaInject, ElemWatchStoreInject } from '../infra';

export interface Computed {
    readonly $storeState: StoreLocalState;
}

export interface Inject extends ElemStoreMetaInject, ElemWatchStoreInject {}

export interface Methods {
    $storeCommit(state: StoreLocalState, { global = false }?: { global?: boolean } = {}): void;
}
