import {AppEntityElem, traverse} from '@goodt-common/utils';

type AppEntityPage = {
    id: string;
    title: string;
    content: AppEntityElem[];
    isTemplate: boolean;
    templatePageId: string;
};

type ElemPatch = (elem: AppEntityElem, parent: AppEntityElem) => void;

/**
 * Patches page json
 * @effect page
 * @param {AppEntityPage} page
 */
export function patchPage(page: AppEntityPage);

export function patchPageContent(content: AppEntityElem[]): void;
