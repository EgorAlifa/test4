<template>
    <div>
        <control-layout v-bind="{ prop, colSize }">
            <template #label="{ getLabel }">
                <!--
                @slot label slot
                -->
                <slot v-bind="{ getLabel }" />
            </template>
            <template #hint="{ getHint }">
                <!--
                @slot hint slot
                -->
                <slot name="hint" v-bind="{ getHint }" />
            </template>
            <template #control>
                <textarea
                    class="textarea textarea-small w-12-12"
                    :class="{...controlCl, 'resizable': resizeBoth }"
                    v-bind="attrs"
                    v-on="listeners"
                />
            </template>
        </control-layout>
    </div>
</template>
<script>
import { ControlMixin } from './utils/index';

export default {
    mixins: [ControlMixin],
    props: {
      resizeBoth: {
          type: Boolean,
          default: false
      }
    },
    methods: {
        valueExtract: (event) => event.target.value
    }
};
</script>
<style scoped>
.textarea.resizable {
    resize: both;
}
</style>
