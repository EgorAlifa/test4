<template functional>
    <div>
        <div class="row row-hgap-l1">
            <div v-if="scopedSlots.label && scopedSlots.label() != null" class="col col-vmid text-truncate">
                <!--
                @slot label slot
                -->
                <slot name="label"></slot>
            </div>
            <div class="col col-vmid col-auto">
                <component :is="injections.tooltip">
                    <template #target="{ binds, events }">
                        <div :class="$style.hint" class="icon cursor-help" v-bind="binds" v-on="events">
                            <i class="mdi mdi-18px mdi-help-circle-outline"></i>
                        </div>
                    </template>
                    <div>
                        <!--
                        @slot hint slot
                        -->
                        <slot></slot>
                    </div>
                </component>
            </div>
        </div>
    </div>
</template>
<script>
import { Tooltip } from 'goodteditor-ui';

export default {
    inject: {
        tooltip: {
            default: Tooltip
        }
    }
};
</script>
<style module lang="pcss">
.hint {
    width: fit-content !important;
    height: fit-content !important;
}
</style>
