<template>
    <div>
        <control-layout v-bind="{ prop, colSize }">
            <template #label="{ getLabel }">
                <!--
                @slot label slot
                -->
                <slot v-bind="{ getLabel }" />
            </template>
            <template #hint="{ getHint }">
                <!--
                @slot hint slot
                -->
                <slot name="hint" v-bind="{ getHint }" />
            </template>
            <template #control>
                <input class="input input-small w-12-12" :class="controlCl" v-bind="attrs" v-on="listeners" />
            </template>
        </control-layout>
    </div>
</template>
<script>
import { ControlMixin } from './utils/index';

export default {
    mixins: [ControlMixin,],
    props: {
        /**
         * @model
         */
        value: {
            type: [Number, String]
        }
    },
    methods: {
        /**
         * @public
         * @param event
         * @return {*}
         */
        valueExtract: (event) => event.target.value
    }
};
</script>
