<template>
    <div class="row">
        <div class="col" :class="colSizeCl">
            <ui-container>
                <ui-has-two-columns>
                    <template #left>
                        <ui-input-cp v-model="model.color" :disabled="disabled">Цвет</ui-input-cp>
                    </template>
                    <template #right>
                        <ui-input-units v-model="model.fontSize" :units="units" :disabled="disabled">
                            Размер
                        </ui-input-units>
                    </template>
                </ui-has-two-columns>
                <ui-has-two-columns>
                    <template #left>
                        <ui-input-auto v-model="model.fontFamily" :disabled="disabled">Шрифт</ui-input-auto>
                    </template>
                    <template #right>
                        <ui-select v-model="model.fontWeight" :options="fontWeightOptions" :disabled="disabled">
                            Насыщенность
                        </ui-select>
                    </template>
                </ui-has-two-columns>
            </ui-container>
        </div>
    </div>
</template>
<script>
import { FontWeightOptions, SizeUnits } from '@goodt-wcore/panels';
import { ControlMixin, ObjectControlMixin, createFont } from './utils';
import UiInputUnits from './InputUnits.vue';
import UiInputAuto from './InputAuto.vue';
import UiInputCp from './InputCp.vue';
import UiSelect from './Select.vue';
import UiContainer from './Container.vue';
import UiHasTwoColumns from './HasTwoColumns.vue';

export default {
    components: { UiInputUnits, UiInputAuto, UiInputCp, UiSelect, UiHasTwoColumns, UiContainer },
    mixins: [ControlMixin, ObjectControlMixin],
    props: {
        /**
         * Options Array[String]
         */
        units: {
            type: Array,
            default: () => SizeUnits
        },
        /**
         * Options Array[Object] ~ [ { value:'', label: '' } ]
         */
        fontWeightOptions: {
            type: Array,
            default: () => FontWeightOptions
        }
    },
    computed: {
        colSizeCl() {
            return `col-${this.colSize}`;
        },
    },
    methods: {
        /**
         * @public
         */
        createSettings(settings = createFont()) {
            return createFont(settings);
        }
    }
};
</script>
