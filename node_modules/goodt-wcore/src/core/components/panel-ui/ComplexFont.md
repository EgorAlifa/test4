```vue
<template>
    <ui-container>
        <pre>{{ prop }}</pre>
        <ui-complex-font v-model="prop" v-bind="binds" />
    </ui-container>
</template>
<script>
import ui from './index.js';
import { SizeUnits, FontWeightOptions } from '@goodt-wcore/panels';

export default {
    components: { ...ui },
    data: () => ({
        prop: {},
        binds: {
            units: SizeUnits,
            fontWeightOptions: FontWeightOptions
        }
    })
};
</script>
```
