<template>
    <div class="shim" :style="{ zIndex}">
        <slot name="overlay">
            <div v-if="overlay" class="shim-overlay"></div>
        </slot>
        <div class="shim-content w-100 h-100 d-flex flex-center">
            <!--
            @slot shim content
            @example Shim content
            -->
            <slot></slot>
        </div>
    </div>
</template>
<script>
export default {
    props: {
        overlay: {
            type: Boolean,
            default: false
        },
        zIndex: {
            type: Number,
            default: 1
        }
    }
};
</script>
