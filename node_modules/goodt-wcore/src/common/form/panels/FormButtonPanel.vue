<template>
    <ui-panel-container>
        <ui-select
            class="p"
            v-model="props.action"
            :options="descriptor.props.action.options"
            @change="propChanged('action')">
            {{ descriptor.props.action.label }}
        </ui-select>
        <ui-input v-model="props.label" @change="propChanged('label')">
            {{ descriptor.props.label.label }}
        </ui-input>
    </ui-panel-container>
</template>
<script>
import { Panel } from '@goodt-wcore/core';

export default {
    extends: Panel,
    data: () => ({
        $meta: { name: 'FormButtonSettings', icon: 'gesture-tap-button' }
    })
};
</script>
