import { SDK } from '@goodt-common/dremio';
import Keycloak from './Keycloak';

class KeycloakDremio extends Keycloak {
    private _cache: {
        permissions: any;
        userPermissions: any;
    };
    private _clearCache(): void;

    dremio: SDK;
}

export default KeycloakDremio;
