import { ComponentOptions } from 'vue';

export interface IDatasetPanelOptions {
    isMultiple?: boolean;
    prop?: string;
    mixins?: ComponentOptions[];
    meta?: IDatasetPanelOptions;
}

export function useDatasetPanels({
    isMultiple,
    prop,
    mixins,
    meta
}: IDatasetPanelOptions): (() => Promise<ComponentOptions>)[];

export function useDatasetPanel({
    isMultiple,
    prop,
    mixins,
    meta
}: IDatasetPanelOptions): () => Promise<ComponentOptions>;


export { default as DatasetPanel } from './DatasetPanel.vue';

export const VariablePanelAsync = () => import('./VariablePanel.vue');
export const DeviationsPanelAsync = () => import('./DeviationsPanel.vue');
