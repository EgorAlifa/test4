import { BaseApiServiceBase } from './BaseApiServiceBase';

export class BaseApiService extends BaseApiServiceBase {
    /**
     * If throw api service error
     *
     * @type {boolean}
     * @protected
     */
    protected _globalError: boolean;
}
