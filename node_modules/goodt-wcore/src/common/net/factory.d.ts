import { ITransport, ITransportOptions } from './types';

/** @type {symbol} */
export const HttpTransportSymbol: symbol;
/** @type {symbol} */
export const HttpAuthTransportSymbol: symbol;
/**
 * @type {Map<symbol, Function>}
 */
export const TransportFactoryMap: Map<symbol, Function>;

export function createTransport(transportId: symbol, options?: (() => ITransportOptions) | Record<string, any>, extraOptions?: { globalError?: boolean }): ITransport;
