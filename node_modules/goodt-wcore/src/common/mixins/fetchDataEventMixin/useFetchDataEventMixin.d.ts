import { VueConstructor } from 'vue';
import { IDescriptorProps } from '@goodt-wcore/core';

interface Data {
    descriptor: {
        props: IDescriptorProps;
    }
}

interface Methods {
    listenFetchDataEvent(event: string): void;
}

export interface IFetchDataEventMixin extends VueConstructor, Data, Methods {}

export function useFetchDataEventMixin(options: { methodName: string }): IFetchDataEventMixin
