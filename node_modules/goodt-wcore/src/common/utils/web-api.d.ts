export type FileDownloadOptions = {
    filename: string
};
/**
 *
 * @param {Blob} blob
 * @param {FileDownloadOptions} options
 * @param [target=window]
 */
export function downloadBlobAsFile(blob: Blob, options: FileDownloadOptions, { URL, document }: { URL: URL, document: Document });
