const { dependencies } = require('./package.json');

// prettier-ignore
const CorePackages = Object.keys({ ...dependencies })
    .filter((key) => key.startsWith('@goodt-'))

module.exports = CorePackages;
