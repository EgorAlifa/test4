export const DEFAULT_BOOK_NAME = 'Goodt table data';
export const MAX_SHEET_NAME_LENGTH = 31;

export const BookType = {
    XLSX: 'xlsx',
    CSV: 'csv'
};

export const Events = Object.freeze({
    REFRESH: 'refresh'
});
