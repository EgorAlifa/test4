/* eslint-disable no-magic-numbers */
export const UserRole = Object.freeze({
    RESPONSIBLE: 1,
    HEAD: 2,
    MENTOR: 3,
    FORBIDDEN: 0
});

export const TaskTypeId = Object.freeze({
    PERFORMANCE_MAP: 1,
    INDICATOR_KPE: 3,
    PRIORITY_GOAL: 4
});

export const ONCE_OPTION = 1;

export const STRING_VALUE_TRUE = 'true';

export const WorkMode = Object.freeze({
    STANDARD: 'standard',
    SELECT: 'select'
});
