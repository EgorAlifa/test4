/* Gauge widget styles */

.gauge-container {
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
}

.gauge-value {
    text-align: center;
    line-height: 1;
}

.gauge-label {
    text-align: center;
}

